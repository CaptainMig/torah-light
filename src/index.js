import React from 'react';
import ReactDOM from 'react-dom/client';
import TorahLight from './TorahLight';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TorahLight />
  </React.StrictMode>
);
